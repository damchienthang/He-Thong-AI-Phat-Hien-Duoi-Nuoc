# Chờ API của Thành

Backend FastAPI và AI inference thuộc Thành. Dashboard kết nối WebSocket trực tiếp từ trình duyệt; xem `../docs/HANDOFF.md` để thống nhất dữ liệu bàn giao.

Chưa có backend thật trong thư mục này. Không có endpoint được giả lập thành hệ thống AI đang hoạt động.
